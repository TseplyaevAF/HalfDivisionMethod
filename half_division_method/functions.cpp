#include "functions.h"

// функция под буквой а
double f1(double x) {
	return 3 * x + pow(5, x) + 6;
}

// функция под буквой б
double f2(double x) {
	return 4 * pow(x, 3) - 5 * pow(x, 2) - 2 * x + 2;
}